<p>Données du formulaire {{ title }}</p>
<table style="margin-bottom:40px">
    {% for field in data %}
    <tr>
        <th style="background:#5f9be3;color:#fff;float:left;font-weight:bold;margin-right:10px;padding:5px;">{{ field.label }}</th>
        <td style="margin:2px 0;padding:5px 0;">{{ field.value }}</td>
    </tr>
    {% endfor %}
</table>

<p>Adresse du formulaire <a href="{{ url }}">{{ url }}</a></p>